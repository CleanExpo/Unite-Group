// Serve the CEO Command Center at the locale-prefixed URL
// The i18n middleware redirects /dashboard/ceo → /en/dashboard/ceo
// This file ensures the page actually renders at that URL.
export { default } from '../../../dashboard/ceo/page';
