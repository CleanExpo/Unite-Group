# Session Handoff — 2026-07-16 11:45 AEST

## 1. Summary
**State: SHIPPED.** Definition-of-Done: all five session PRs are open, green drafts on pushed
tips — **#853** (runner claim/release plane + tmux harness, dark), **#854** (B2 wall feed,
honest states), **#855** (opened BY the runner itself during the live E2E proof), **#856**
(rate-limiter boundary fix), **#857** (contacts drift-baseline migration). The executor loop
was **proven running end-to-end on a local Supabase stack** (queued → claimed → executed by
headless claude → draft PR in 75s; prod untouched). Estate systems audited: nightshift-work
fixed after 8 nights of silent aborts (proven under launchd same-day, exit 0), Hermes gateway
restarted clean, docs workflows diagnosed healthy (first cron fire Sat 18/07 UTC). Wayfinder
map UNI-2379 fully worked: 3 research + 2 grill + 2 task tickets resolved; grills decided
under the founder's /goal continuous-build directive with seeded recommendations (override
anytime). Phase 0 this handoff: type-check 0, lint 0, vitest 3,413/3,413 fresh on tip
`dd7d44f4`; build proven by #853's CI on the same tip. (`scripts/handoff-loop.sh` absent in
this repo — gates run individually.)

## 2. Where it started
Resumed from the 15/07 22:34 handoff; founder answered grill Q1 (cockpit-first), took
"default" on Q2–Q7, then issued /goal (continuous build, no stopping). Repo:
CleanExpo/Unite-Group, worktree `.claude/worktrees/nexus-runner-v1`, branch
`feat/nexus-runner-v1` off `origin/main` (`02abfa8d`).

## 3. Decisions locked + what shipped
**Decisions (grill Grills/13 + map UNI-2379, all recorded on Linear with provenance):**
cockpit-first identity; Mac-resident tmux runner (claude CLI is NOT TCC-silent under launchd
— nightshift precedent); Anthropic-only on Max OAuth (FabSol routing Board-escalated, brief
drafted on UNI-2386); marketing layer wired-in (in-Nexus media production NO-GO); L2 autonomy
ceiling = draft PR only; first job = top L2 queue item w/ heartbeat fallback; event taxonomy =
convention (`status` + verb in tool_name, codes in target); pre-demo founder sitting order.
**Shipped (all draft PRs, base main, CI green):**
- #853 `dd7d44f4` — cc_tasks claim migration (INERT) + dark routes `/api/agents/runner/{claim,release}`
  + `runner-claim.ts` accessors + `scripts/nexus-runner/` harness. +26 tests.
- #854 `2f8af782` — B2 wall feed (`agent-events-wall.ts` + `AgentEventsWallTile` on operations
  page): table-missing → "Wall dark", empty → "runner not armed", never fake-live. +21 tests.
- #855 — runner-authored E2E proof PR (one README line; leave as evidence, merge optional).
- #856 `9d5ed665` — `classifyTier()` 'ai' tier no longer captures `/api/agents/*` (was
  429-dropping runner events at 5 req/min). +6 tests pin the boundary.
- #857 `0eb97d0f` — `20260629990000_contacts_drift_baseline.sql`: fresh `supabase db reset`
  works again (was broken on main by prod-only drift columns; proven exit 0 locally).
**Estate fixes applied outside the repo:** `~/.claude/night-shift/run-work.sh` volume-guard
allowlist (Claude/Antigravity images; proven exit 0 under launchd); Hermes gateway kickstart.
**Vault/Linear/memory:** wiki `nexus-operating-model` §15 + vault commits (`d0732a3`, ingest
commit); corpus synced 566/587; memory `claude_cli_launchd_tcc.md`; Linear UNI-2379–2389 all
current.

## 4. Key files
| File | Status |
|---|---|
| apps/web/supabase/migrations/20260716010000_cc_tasks_claim.sql | Created (PR #853, INERT until founder db push) |
| apps/web/src/lib/command-centre/runner-claim.ts (+tests) | Created (PR #853) |
| apps/web/src/app/api/agents/runner/{claim,release}/route.ts (+tests) | Created (PR #853) |
| scripts/nexus-runner/{runner.mjs,run.sh,README.md} | Created (PR #853) |
| apps/web/src/lib/command-centre/agent-events-wall.ts + AgentEventsWallTile.tsx | Created (PR #854, other worktree) |
| apps/web/src/lib/middleware/rate-limit.ts (+new tests) | Modified (PR #856, other worktree) |
| apps/web/supabase/migrations/20260629990000_contacts_drift_baseline.sql | Created (PR #857, other worktree) |
| ~/.claude/night-shift/run-work.sh | Modified in place (machine-level fix, not repo) |

## 5. Running state
Branch `feat/nexus-runner-v1`, tree clean, tip `dd7d44f4` = pushed PR tip. No servers or
runners left running (E2E stack stopped, `docker ps` clean; dev server + runner pids killed).
One FOREIGN stash (`uni-2234`) — not mine, untouched. Sibling worktrees `b2-wall-feed`,
`agents-rate-tier`, `contacts-drift-baseline` hold the other PRs' branches (clean, pushed).
An hourly ops-watch wakeup loop from the /goal session is CANCELLED as of this handoff.
Safe to stop.

## 6. Verification
- This handoff: `cd apps/web && pnpm run type-check && pnpm run lint && pnpm vitest run`
  → 0 / 0 / 3,413 passed (fresh, this session). Build: PR #853 CI green on `dd7d44f4`
  including Playwright; #854/#856/#857 apps/web CI jobs all `pass` (session-verified via
  `gh pr checks`). No `.handoff-logs` file — handoff-loop.sh absent in this repo.
- E2E evidence: `/tmp/nexus-e2e-dev.log`, `/tmp/nexus-e2e-runner.log`; PR #855.
- Post-arming smoke: the checklist in `scripts/nexus-runner/README.md`.

## 7. Deferred + open questions
- **Merge directive for #853/#854/#856/#857** (Owner: Phill · Blocking: everything below ·
  Why: merge classifier requires a live PR-specific typed directive; `gh pr ready` was denied
  this session). #855 optional.
- **UNI-2385 arming sitting** (Owner: Phill · Blocking: live loop · Why: founder-gated —
  F1 flip → `supabase db push` (three dark migrations now: cc_agent_events, cc_tasks_claim,
  drift-baseline) → `vercel env add AGENT_EVENTS_SECRET` → `~/.claude/nexus-runner.env` →
  tmux start. Order in `scripts/nexus-runner/README.md`).
- **UNI-2386 FabSol Board decision** (Owner: Phill/Board · brief drafted on the ticket).
- **Docs-workflow baseline policy** (Owner: Phill · UNI-2387 comment · first cron fire Sat
  18/07 UTC; supervise the automation's first non-draft PR).
- **Nightshift 02:30 scheduled fire tonight** (Owner: next session · verify
  `~/.claude/night-shift/reports/work-*.md` shows a clean run, not just the manual kickstart).

## 8. Pick up here
**Start here:** after Phill's merge directive lands, verify merged CONTENT on origin/main
(per feedback_merge_race_verify_content), then walk UNI-2385 with him.
**Do not redo:** the five PRs; the grill/map (all decisions recorded on Linear); the E2E proof;
the nightshift/gateway fixes; the wiki ingest.
**First command to run:** `gh pr view 853 --json state,mergedAt && gh pr view 854 --json state,mergedAt`

## 9. Risk notes
- Grills Q2–Q7 + lifecycle/watch decisions were resolved by standing-default under the /goal
  directive — provenance is on each Linear ticket; any can be overridden before arming.
- The E2E proof is LOCAL evidence; prod behaviour after arming still needs the README smoke.
- Rate-limit fix (#856) is inferred to clear the 429 drops (tests pin tiers); confirm on the
  first armed run.
- Nightshift proven via manual kickstart + one clean scheduled context pending (tonight).
- `.env.local` used for the E2E was machine-local and deleted with the stack; no secrets in
  any PR (gitleaks green on #857; grep-swept #853).

## 10. Handoff quality check
No faked verification (every gate result is a this-session tool output; subagent claims were
independently re-verified via `gh pr checks`); no hidden running processes (E2E stack + watch
loop stopped/cancelled); completed (5 PRs, map, audit, fixes, proofs) vs deferred (merge
directive, arming sitting, Board call) cleanly separated. SHIPPED classification honest:
every change is committed, pushed, and PR'd on a green tip.

Handoff complete. Next safe action: give the typed merge directive for #853/#854/#856/#857, then run the UNI-2385 arming sitting to bring the proven loop live.
