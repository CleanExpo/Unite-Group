# Spec — Mission Control: Obsidian panel & vault scan (thin slice)

> Generated by `brain-scope` on 2026-06-25. **This is a plan, not code.**

## Source

- **Task note:** `/Users/phillmcgurk/2nd-brain/Wiki/analyst/2026-06-21-120047-obsidian-connection-proof.md`
  - A connection-proof smoke test written by `swarm.analyst._mirror_obsidian` (config from `~/.hermes/.env`) confirming the analyst path can now write notes into the real `brain-1` vault — the prior "No Obsidian" issue is resolved.

## Context (ranked, vault-only)

1. **`Sketches/06-unite-agent-os-command-center-layout.md`** — `status: ready-for-build`. Defines the "Mission Control Agent OS" command center, which includes an **`Obsidian ●` connection-status dot** in the command rail and a **read-only vault-scan endpoint** reading `/Users/phillmcgurk/2nd-brain`. The proof-note is exactly what lights up that dot. *(Highest relevance — ratified, with an explicit build path + non-goals.)*
2. **`Sources/2026-06-24-agent-os-obsidian-command-center-research.md`** — research backing sketch 06 (Chase/Sakana-style command center, adapted to Unite/Hermes infra).
3. **`Sketches/05-nexus-skills-os.md`** — `status: draft`, appetite 2w. Broader "Skills OS" that uses Obsidian (`/Users/phillmcgurk/2nd-brain`) as the operating brain. Related but larger scope; not part of this slice.

## Decisions (from the scoping dialog — all the recommended option)

| Question | Choice |
|---|---|
| Scope | **Thin slice** — read-only vault-scan endpoint + `Obsidian ●` connection dot + a minimal panel showing the scan result |
| Approach | **Follow sketch 06's default path** — new `/agent-os` screen in `apps/workspace`, reuse the Unite theme, static layout first, read-only scan of the vault |
| Scan depth | **Read-only metadata** — folders, file counts, last-modified; exclude `.git/`, `.agentic_nexus/`, `Backups/`. No note contents read. |

## Scope

**In scope**
- A read-only vault-scan API in `apps/workspace` that reads `/Users/phillmcgurk/2nd-brain` and returns **metadata only**: folder list, per-folder `.md` counts, most-recent modified time, total note count — excluding `.git/`, `.agentic_nexus/`, `Backups/`.
- An `Obsidian ●` connection-status dot in the command rail (green when the scan succeeds, red/amber when it fails).
- A minimal panel rendering the scan summary (folders + counts + last-sync time), using the Unite OLED theme (`#050505`, thin cyan borders, small uppercase labels).

**Out of scope / No-gos**
- Full command-center shell (intelligence graph, domain cards, inspector, command composer) — later slices.
- **Full content indexing** or reading note bodies; **live file-watch** sync.
- No Sakana Fugu, no Nango / third-party connector platforms, no new external vendors/accounts (sketch 06 + 05 non-goals).
- No DB writes, deploys, PRs, client comms, billing, or secret entry without operator approval.
- No production code from this `brain-scope` pass — this document is the plan only.

## Plan (thinnest end-to-end slice first)

1. **Vault-scan endpoint** — add a read-only API in `apps/workspace` that scans `/Users/phillmcgurk/2nd-brain` and returns metadata only (folders, `.md` counts, last-modified, totals), applying the `.git/ .agentic_nexus/ Backups/` exclusions. Mirror the exclusion logic the vault itself uses (see `brain-scope` skill + `2nd-brain/CLAUDE.md`).
2. **Connection dot** — add the `Obsidian ●` status indicator to the command rail; green on a successful scan, error state otherwise.
3. **Minimal panel** — render the scan summary (folder list + counts + last-sync time). Static layout first.
4. **Theme** — reuse the existing Unite theme variables (`[data-theme='unite']` in `src/styles.css`); OLED `#050505`, thin cyan borders, uppercase labels.
5. **Verify** — `pnpm run build` + a browser screenshot of the panel showing live vault metadata.

Reference notes for a future agent: `Sketches/06-unite-agent-os-command-center-layout.md` (build steps 1/3/5), `Sources/2026-06-24-agent-os-obsidian-command-center-research.md`.

## Open questions / rabbit holes

- **Connection health signal:** does the `Obsidian ●` dot reflect "scan endpoint succeeded" only, or should it also reflect a heartbeat from `swarm.analyst._mirror_obsidian` (the path that wrote the proof-note)? The proof-note suggests the analyst write-path is a separate signal worth surfacing.
- **Endpoint auth / path access:** how `apps/workspace` is permitted to read a local absolute path (`/Users/phillmcgurk/2nd-brain`) in dev vs any deployed context — local-only, or via a configured vault root.
- **Vault root config:** sketch 05 also references an embedded vault at `/Users/phillmcgurk/Unite-Group/docs/brain/2nd Brain` — confirm the canonical scan target is the top-level `/Users/phillmcgurk/2nd-brain` (the `brain-1` vault the proof-note landed in).
