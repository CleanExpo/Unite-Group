#!/bin/bash
# Permanent tombstone for a launcher that previously armed claim, agent, Git,
# push, and Linear-completion behaviour by default.

printf '%s\n' 'Mission Control Linear loop is permanently retired; CRM OWNEST is authoritative.' >&2
exit 2
