# Temporary for the React 19 / Next 16 migration: @visx 3.x peer ranges do not yet declare React 19 compatibility.
# Remove this once the tracked React Compiler/peer-dependency hardening slices replace or upgrade that surface.
legacy-peer-deps=true
