// Empty CSS module — referenced from jest.config.js moduleNameMapper.
// Component code does `import '*.css'` for style-loading at runtime;
// in tests we render to static markup so stylesheets are irrelevant.
module.exports = {};
