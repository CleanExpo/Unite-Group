# Compass — Unite-Group (injected before every user prompt)

**Canonical identity**: Unite-Group only. Superseded/legacy identities are not current aliases and must not be injected into active reasoning.
**Stack**: Next.js 16 App Router + React 19 + Supabase PostgreSQL + Vercel.
**Auth/data**: Supabase PKCE; founder-scoped data uses the current repository contract. Never infer schema from stale context—verify it.
**Design**: Read current design tokens and ratified registers from active `CLAUDE.md` and the implemented CSS modules. Do not inject retired palettes or historical design systems.
**Execution**: Preserve the current `/spm` mission state. Explicit BUILD_AUTHORISED work advances through execution/verification; do not silently return it to planning.
**Truth**: Current evidence outranks memory. Prefer canonical/current sources; exclude superseded and legacy material unless history is explicitly requested.
**Context economy**: Load the minimum current context needed; delegate or retrieve specialist detail only when required.
