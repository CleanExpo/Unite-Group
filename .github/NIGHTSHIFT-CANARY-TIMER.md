# Nightshift canary

Zero-touch TIMER-FIRED pipeline test (StartCalendarInterval, not kickstart) artifact. This draft PR was created unattended by the
nightshift write runner via launchd to prove the full path (op --cache=false →
REST branch → PUT → draft PR) runs with zero permission prompts. Safe to close.
